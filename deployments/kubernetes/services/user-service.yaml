apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: mall-go
spec:
  replicas: 2
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      containers:
      - name: user-service
        image: mall-go/user-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8001
          name: http
        env:
        - name: CONFIG_PATH
          value: "/app/configs/config.yml"
        - name: MYSQL_HOST
          value: "mysql"
        - name: MYSQL_PORT
          value: "3306"
        - name: MYSQL_USER
          value: "root"
        - name: MYSQL_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: password
        - name: CONSUL_ADDRESS
          value: "consul:8500"
        volumeMounts:
        - name: config-volume
          mountPath: /app/configs
        resources:
          limits:
            cpu: 500m
            memory: 512Mi
          requests:
            cpu: 200m
            memory: 256Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 8001
          initialDelaySeconds: 30
          timeoutSeconds: 5
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8001
          initialDelaySeconds: 5
          timeoutSeconds: 1
          periodSeconds: 2
      volumes:
      - name: config-volume
        configMap:
          name: user-service-config
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: mall-go
spec:
  selector:
    app: user-service
  ports:
  - port: 8001
    targetPort: 8001
    name: http
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: user-service-config
  namespace: mall-go
data:
  config.yml: |
    server:
      port: 8001
      readTimeout: 10
      writeTimeout: 10
      mode: release

    database:
      type: mysql
      host: mysql
      port: 3306
      username: root
      password: ${MYSQL_PASSWORD}
      database: mall_user
      logLevel: info

    jwt:
      secret: mall-user-service-secret-key
      expiration: 1440  # 24小时

    redis:
      host: redis
      port: 6379
      password: ""
      db: 0

    consul:
      address: consul:8500
      serviceName: user-service
      tags:
        - go
        - user
        - api
      meta:
        version: v1.0
        environment: prod